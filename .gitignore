node_modules/
server/data/db/*.json
.vercel/
*.log

# Реальный прайс поставщика (закупочные цены) — репозиторий публичный,
# этот файл в него не попадает, чтобы себестоимость не была видна всем.
ARTECO прайс лист.xlsx
